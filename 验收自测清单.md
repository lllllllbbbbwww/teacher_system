# 验收自测清单（对照 PRD + 开发铁律）

> 使用方式：按编号逐项自测，全部通过即达到交付标准。

## 一、数据隔离铁律
- [ ] 注册两个教师账号 A、B，A 录入班级/学生/成绩/考勤/行为。
- [ ] 用 B 的 token 调用列表接口，确认**看不到** A 的任何数据（返回空）。
- [ ] B 尝试用 A 的资源 id 调 `PUT/DELETE`，返回 404/403（不越权）。
- [ ] 代码审计：所有 `from('<业务表>')` 查询均含 `.eq('teacher_id', req.user.id)`，无关联表间接校验。

## 二、接口规范
- [ ] 统一响应：成功返回 `{code:0, data, msg:"success"}`；业务错误 `code≠0`。
- [ ] 入参校验：缺失/非法字段返回 400 及中文提示（如空班级名、性别非法）。
- [ ] 全局异常：触发数据库异常时返回 `{code:500, msg:"服务器内部错误"}`，进程不崩溃。
- [ ] 唯一键冲突：重复学号返回 `{code:409, msg:"数据已存在"}`。

## 三、功能验收（对应 PRD 11 项）
1. [ ] 注册/登录：bcrypt 加密存储；JWT 7 天有效；未登录访问受保护页面跳登录。
2. [ ] 班级 CRUD：增删改查 + 软删除；删除后列表与统计均不再出现。
3. [ ] 学生管理：单条/批量录入；学号同教师下唯一；软删除；按班级筛选。
4. [ ] 考试场次：CRUD + 软删除；归属校验。
5. [ ] 成绩录入：批量提交；分数超满分被拒；同考试同学生覆盖（upsert）。
6. [ ] 成绩查询：列表 + 均分/最高/最低/及格率/优秀率统计；ECharts 柱状图。
7. [ ] 考勤录入：按天提交；同一天同一学生重复提交覆盖；状态 4 类。
8. [ ] 考勤统计：近 30 天迟到+旷课≥3 次预警；明细查询；Excel 导出。
9. [ ] 行为标签库：系统标签不可删；自定义增删；positive/improve 分类。
10. [ ] 行为记录：选学生+标签（≥1）+日期；关联标签正确展示。
11. [ ] 反馈风格：系统 4 风格不可改；自定义增改删。
12. [ ] AI 反馈：勾选维度+风格+周期生成；**内容不编造**（核对真实数据）；1 分钟内同参数幂等；失败不入库。
13. [ ] 反馈记录：历史列表 + 详情；可导出单学生 PDF。

## 四、导出功能
- [ ] 成绩 Excel：文件名 `班级_考试_成绩表_日期.xlsx`，含学号/姓名/分/满分。
- [ ] 考勤 Excel：含状态中文映射；按日期范围筛选。
- [ ] 学生 PDF：中文正常显示（思源黑体），含成绩表 + AI 反馈完整版。

## 五、视觉与交互（铁律 5）
- [ ] 极简办公风：白底浅灰、无花哨动画、布局符合教师使用习惯。
- [ ] 核心交互：表格操作行内编辑/删除、表单弹窗、列表筛选均直观可用。

## 六、不超范围（铁律 3）
- [ ] 无作业模块、无家长端、无无关文件上传功能。

## 七、联调通过标准
- [ ] 后端 `npm run dev` 启动，`/health` 返回 `{code:0,data:"ok"}`。
- [ ] 前端 `npm run dev` 启动，注册→登录→录入全流程走通。
- [ ] 前端 `npm run build` 成功（已验证：13 页 + Layout 全部编译通过）。
