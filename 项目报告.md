# 教师学情智能管理系统 v2.1 — 项目报告

> 文档生成日期：2026-08-27

## 一、项目定位

面向**中小学班主任**的学情管理工具。核心能力：班级 / 学生 / 考试 / 成绩 / 考勤 / 行为标签管理，并**基于真实数据**自动生成家长沟通用的 AI 学情反馈（微信短版 + 完整版），支持 Excel / PDF 导出。

---

## 二、技术栈与端口

### 技术栈（按 README 规定，固定不可变更）

| 层 | 技术 |
| --- | --- |
| 前端 | Vue 3 + Element Plus + ECharts + Pinia + Vue Router + Vite |
| 后端 | Node.js + Express + JWT |
| 数据库 | PostgreSQL（托管在 Supabase） |
| 数据访问 | `@supabase/supabase-js` 链式调用（严禁原生 SQL 拼接）|
| AI | 豆包 / OpenAI 兼容接口 |
| 部署 | 前端 Vercel（历史）→ 后端腾讯云云托管（CloudBase 容器，当前）|

### 端口与地址

| 服务 | 端口 | 说明 |
| --- | --- | --- |
| 后端（本地） | `3000` | `npm run dev`，`localhost:3000` |
| 后端（CloudBase） | `3000` | 容器内 `EXPOSE 3000`，`PORT=3000`，对外由云托管网关映射 |
| 前端（本地） | `5173` | `npm run dev`，Vite 开发服务器 |
| 前端（当前线上）| 80/443（HTTPS）| `https://teacher-system-d1gxqxshe42e830c5-1462222370.tcloudbaseapp.com` |
| 数据库 | 5432（Supabase 托管）| 不直接暴露，后端用 service_role key 访问 |

> 前端生产环境 API 地址**写死**在 `frontend/src/utils/request.js`：
> `https://teacher-backend-303194-11-1462222370.sh.run.tcloudbase.com/api`
> 本地开发走 Vite 的 `/api` 代理到 `localhost:3000`。

---

## 三、目录结构

```
vib_jiaoshi/
├── database/schema.sql      # 11 张表 + 索引 + 唯一约束 + 种子数据
├── backend/                 # Express 后端（源码）
│   └── src/
│       ├── index.js         # 入口：校验 env、初始化管理员、listen
│       ├── app.js           # 中间件 + 路由挂载
│       ├── config/env.js    # 环境变量校验 + Supabase 客户端
│       ├── middleware/      # auth(JWT)、errorHandler
│       ├── routes/          # 13 个路由（auth/class/student/exam/score/...）
│       ├── controllers/     # 13 个控制器（业务逻辑）
│       ├── services/ai.js   # LLM 调用 + Prompt 组装
│       ├── validators/      # Joi 入参校验
│       └── utils/           # 统一响应、错误类
├── backend-deploy/          # 部署用：Dockerfile + package.json（同后端）
├── frontend/                # Vue3 前端
│   └── src/
│       ├── views/           # 21 个页面(.vue)
│       ├── router/          # 路由 + 登录守卫
│       ├── stores/          # Pinia(auth/class/student/common)
│       ├── utils/request.js # axios 封装（注入 token、统一响应）
│       └── layout/          # 主框架(侧边栏+顶栏)
├── README.md / DEPLOY.md     # 文档
└── 验收自测清单.md
```

---

## 四、数据库设计（11 张表）

所有业务表都冗余 `teacher_id`（数据隔离），删除统一用 `deleted_at` 软删除，主键均为 UUID（`pgcrypto` 生成）。

| 表 | 作用 |
| --- | --- |
| `teacher_user` | 教师账号（username/password_hash/role/disabled）|
| `class` | 班级（含 teacher_id）|
| `student` | 学生（含 class_id/teacher_id，学号唯一约束）|
| `exam` | 考试场次（科目/满分/时间）|
| `score` | 成绩（exam_id+student_id 唯一）|
| `attendance` | 考勤（normal/late/leave/absent）|
| `feedback_style_tag` | 反馈风格标签（温和鼓励/直白客观/数据详实/简洁高效，系统级为 null）|
| `behavior_tag` | 行为标签库（positive/improve，系统级为 null）|
| `student_behavior` | 学生行为记录 |
| `behavior_tag_rel` | 行为-标签关联（多对多）|
| `student_feedback` | AI 反馈记录（含数据快照 JSONB）|

**种子数据**：4 个系统反馈风格标签 + 8 个系统行为标签。

---

## 五、后端逻辑

### 1. 鉴权流程
- `POST /api/auth/register`：用户名唯一校验 → `bcrypt.hash(10)` 存密 → 写入 `teacher_user`。
- `POST /api/auth/login`：查用户 → `bcrypt.compare` → 校验 `disabled` → 签发 JWT（`jsonwebtoken`，`expiresIn=7d`），payload 含 `{sub, username, role}`。
- `authMiddleware`：从 `Authorization: Bearer` 或 `?token=` 取 token → `jwt.verify` → 挂载 `req.user`。**所有业务接口都必须过此中间件**。
- `requireAdmin`：校验 `role==='admin'`。
- **系统初始化**：启动时若设了 `INIT_ADMIN`，自动创建/同步管理员账号（方便改 `.env` 密码后重启生效）。

### 2. 数据隔离铁律
每个查询第一条件都是 `eq('teacher_id', req.user.id)`，禁止通过关联表间接校验权限——保证 A 老师看不到 B 老师的数据。

### 3. 核心业务接口（前缀 `/api`）

| 模块 | 主要接口 |
| --- | --- |
| 班级 | `/classes` CRUD |
| 学生 | `/students` CRUD（软删除）|
| 考试 | `/exams` CRUD |
| 成绩 | `/scores/batch`（批量导入）、`/scores` 查询 |
| 考勤 | `/attendance/batch`、`/attendance`、`/attendance/summary` |
| 行为 | `/behaviors/tags`、`/behaviors/records` |
| 风格标签 | `/style-tags` CRUD |
| 仪表盘 | `/dashboard` |
| 数据分析 | `/analytics/exam-overview`（均分/排名/及格率/学科能力分布）|
| 用户管理 | `/admin/users`（仅管理员）|
| 导出 | `/export/exam-excel`、`/export/attendance-excel`、`/export/student-pdf` |

### 4. AI 学情反馈（最有价值的部分）
- 接口：`POST /api/feedback/generate`。
- 流程：
  1. **幂等保护**：1 分钟内同参数直接返回上次结果。
  2. 查风格标签指令（系统级 + 本人自定义）。
  3. `aggregate()` **聚合真实数据**：按时间范围（近2周/近1月）拉成绩、行为标签、考勤，生成文字摘要。
  4. `buildPrompt()` 组装严格 Prompt（**禁令：严禁编造未出现的数据；无数据维度跳过不提**）。
  5. `callLLM()` 调大模型（默认豆包，兼容 OpenAI；超时 30s，重试 1 次）。
  6. 解析返回 JSON（short_version 微信版 / full_version 完整版）。
  7. **降级容错**：AI 不可用或格式异常时，自动用模板生成基础反馈，**绝不报 500**。
  8. 入库 `student_feedback`（含 `source_snapshot` 数据快照）。
- 另有 `getStudentCard`（学生档案卡：最近成绩/趋势/出勤率/评级/近6次成绩历史）与 `listFeedback`（历史）。

### 5. 导出逻辑
- **Excel**（exceljs）：班级成绩表、考勤汇总，带中文文件名（UTF-8 编码）。
- **PDF**（pdfmake）：学生个人学情，使用仓库内自带的中文字体 `NotoSansCJKsc-Regular.otf` 解决中文乱码。

### 6. 统一响应 & 错误处理
- 所有响应 `{ code, data, msg }`，`code=0` 成功。
- `Joi` 入参校验；`errorHandler` 全局捕获；404 兜底。

---

## 六、前端逻辑

### 1. 技术要点
- **Vue 3 + Vite**，组件按需 + `Element Plus` 全套 UI，`ECharts` 画图，`Pinia` 状态管理，`vue-router` 路由。
- `axios` 封装（`request.js`）：请求拦截注入 `localStorage` 的 token；响应拦截只取 `data`，统一错误提示；**401 自动清 token 并跳 `/landing`**。

### 2. 路由与登录守卫（`router/index.js`）
- 公开页：`/login`、`/register`、`/landing`（落地页）。
- 受保护页：工作台、学生/班级/成绩/考勤/行为/反馈管理、用户管理（admin）。
- 守卫逻辑：未登录访问受保护页 → 跳转登录；已登录访问登录/落地 → 跳转工作台；`/` → 按登录态分流 landing/dashboard。

### 3. 页面功能（21 个）
- 落地页 `LandingPage`、登录、注册。
- 工作台 `Dashboard`（概览）。
- 班级 / 学生管理（`ClassManage` / `StudentManage`）。
- 成绩管理（`ScoreManage` 录入、`ScoreQuery` 查询）。
- 考勤（`AttendanceEnter` 录入、`AttendanceManage`、`AttendanceStats` 统计）。
- 行为（`BehaviorRecord` 记录、`BehaviorTagManage` 标签库）。
- 课后反馈（`FeedbackGenerate` 生成、`FeedbackManage`、`FeedbackRecords`、`StudentProfile` 档案）。
- 风格标签管理（`StyleTagManage`）。
- 用户管理（`AdminUsers`，admin 专用）。

### 4. 状态管理（Pinia）
- `auth.js`：token/username/role + 登录态。
- `class.js` / `student.js` / `common.js`：班级、学生、通用字典缓存。

---

## 七、部署现状（当前）

| 部分 | 当前状态 |
| --- | --- |
| 数据库 | Supabase 云库（不变）|
| 后端 | **腾讯云云托管（CloudBase 容器）**，`teacher-backend` 服务，基于 `node:22-alpine` 镜像（因 `@supabase/supabase-js` 需 Node 22+ 原生 WebSocket），国内访问顺畅 |
| 前端 | 历史曾部署 Vercel（指向 Render 后端），**建议删除旧 Vercel 项目**，只用腾讯云新前端地址 |

**关键部署踩坑（已解决）**：早期用 `node:20-alpine` 构建成功但启动崩溃，报错 `Node.js detected but native WebSocket not found`；升级到 `node:22-alpine` 后正常。

---

## 八、安全与规范要点
1. 密码 `bcrypt` 加盐哈希，JWT 鉴权，无明文密码落库。
2. 严格数据隔离：`teacher_id` 贯穿所有查询。
3. 禁止原生 SQL，统一用 Supabase 链式调用（防注入）。
4. `.env` 不入库（gitignore），仅提交 `.env.example`。
5. AI 调用全程容错降级，绝不因外部服务挂掉而让页面崩。
