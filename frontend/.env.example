# 前端环境变量示例（复制为 .env 用于本地开发，或 .env.production 用于生产）
# 本地开发可不填，前端走 vite.config.js 的 /api 代理到 localhost:3000
# 生产部署时由平台注入 VITE_API_BASE

# 后端公网地址（生产必填，例如 https://teacher-backend.koyeb.app）
VITE_API_BASE=
